import 'package:puer_effect_handlers/puer_effect_handlers.dart';
import 'package:test/test.dart';

void main() {
  group('IsolateEffectHandler', () {
    test('exists and can be extended', () {
      // Just verify the class exists and can be extended
      // Full isolate tests are complex and can be flaky
      expect(IsolateEffectHandler, isNotNull);
    });
  });
}
